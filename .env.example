# OpenRouter API Key (optional - only needed if using OpenRouter in config.yaml)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Agent mode: "offline" or "online"
# offline: Uses only local RAG from downloaded docs
# online: Combines web search with local RAG
AGENT_MODE=offline

# Optional: Enable memory for multi-turn conversations
ENABLE_MEMORY=false
